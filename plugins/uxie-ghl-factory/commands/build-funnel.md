---
description: Build a GoHighLevel funnel or page (custom HTML, tracking, SEO)
---
Use the ghl-funnels-pages skill for: $ARGUMENTS
Non-negotiables: write rails first; contract order (recon → brief → intake →
blueprint → approval → execute → verify); pipelines are out of scope here —
route pipeline requests to the ghl MCP server's public v3 actions.
