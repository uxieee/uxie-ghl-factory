{
  "_note": "update_appointment_status — verified live 2026-07-16/17f. WHICH appointment it targets is IMPLICIT and cannot be named: in an appointment-triggered workflow it acts on the TRIGGERING appointment (safe); entered ANY other way (e.g. payment_received, a tag) GHL acts on the appointment with the LATEST START TIME the contact is carrying — NOT most-recently-created, NOT the paid product's appointment (proven by controlled A/B, 2026-07-17f). If a contact can hold >1 appointment, do NOT rely on this: either make the workflow appointment-triggered, or target explicitly via custom_code (GET /contacts/{id}/appointments, pick the one you mean, PUT /calendars/events/appointments/{id}) — see catalog/step-examples/custom_code.json. An API PUT to that endpoint DOES fire status triggers. status_type is 'confirmed' | 'cancelled'. NOTE: GHL has NO native 'rescheduled' status — detect a reschedule by triggering on the appointment event with no status filter plus an if_else on {conditionType:'appointment', conditionSubType:'appointmentRescheduled', conditionOperator:'is', conditionValue:'true'}.",
  "id": "<uuid-001>",
  "order": 2,
  "attributes": {
    "category": "appointment",
    "type": "update_appointment_status",
    "status_type": "confirmed"
  },
  "name": "Confirm Appointment",
  "type": "update_appointment_status",
  "next": "<uuid-002>"
}
