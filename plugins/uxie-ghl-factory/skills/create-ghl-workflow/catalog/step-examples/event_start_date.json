{
  "_variantRules": {
    "_note": "This example is a SINGLE harvested capture, so for each attribute below it pins ONE value of a closed set GHL itself declares. The other values are real and are NOT demonstrated here — do not read this example as evidence that they do not exist. `declaredValues` is quoted from the recovered builder source named in `source`; where a variant takes different companion fields, this catalog has no capture of them. See update_contact_field.json for the worked case that motivated these annotations.",
    "rules": [
      {
        "attribute": "event_start_type",
        "declaredValues": [
          "datetime",
          "custom_field",
          "recurring"
        ],
        "source": "sniffs/bundle/recovered-source/src/models/actions/EventStartDate.ts::EventStartType",
        "note": "The `recurring` variant additionally takes a RecurringType of ['day_week','day_month'] (EventStartDate.ts::RecurringType), uncaptured here.",
        "pinnedByThisExample": "custom_field"
      }
    ]
  },
  "id": "<uuid-001>",
  "order": 0,
  "attributes": {
    "type": "event_start_date",
    "event_start_type": "custom_field",
    "value": "{{task.dueDate}}"
  },
  "name": "Task Due Date",
  "type": "event_start_date",
  "next": "<uuid-002>"
}
